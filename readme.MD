## This is only for the pre-group testing only.
Backend is falskapp

run command:
```
pipenv install
pipenv run flask run
```
Frontend is reactapp

run command:
```
npm install
npm start
```

user features are not included in the file

API and CSS/HTML testing only. 